var restful = require('../../../index.js'),
    mongoose = require('mongoose');

var User = new restful.Model({
  title: 'user',
  methods: ['get', 'post', 'put', 'delete'],
  schema: mongoose.Schema({
    username: 'string',
    password: 'string',
  }),
});

exports = module.exports = User;
